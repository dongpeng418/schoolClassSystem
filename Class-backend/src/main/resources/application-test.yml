spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://crevaluedb.m.mysql:3306/crevaluedb?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: edi
    password: cityhouse
    sql-script-encoding: UTF-8
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy/MM/dd HH:mm:ss
    time-zone: GMT+8
    locale: zh_CN
  redis:
    host: 192.168.3.142
    port: 6379
    timeout: 10s
    database: 2
  http:
    encoding:
      force: true
      force-request: true
      force-response: true
  task:
    execution:
      pool:
        core-size: 10
        max-size: 20
        queue-capacity: 64
      thread-name-prefix: AVS-EXE-
    scheduling:
      pool:
        size: 5
      thread-name-prefix: AVS-SCH-
  freemarker:
    check-template-location: false
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 35MB

server:
  port: 8066
  tomcat:
    uri-encoding: utf-8
    basedir: ./data/tomcat

mybatis:
  type-aliases-package: cn.com.school.classinfo.model
  mapper-locations: classpath:mybatis/*.xml

logging:
  level:
    cn:
      cityre:
        crevalue:
          mapper: debug

avs:
  api:
    api-key: 4IUFP6jkGPPNSmUUERNSn2
    city-house-http-prefix: http://api.cityhouse.cn
    cre-value-http-prefix: http://api.crevalue.cn
    cre-value-https-prefix: https://api.crevalue.cn
    cre-value-test-prefix: http://test.api.creprice.cn/
    batch-start-notify: SMS_148835071
    batch-finish-notify: SMS_157350532
  path:
    base: /App/java_app/crevalue-open/data/tenant/
    company: company/
    report-config: report-config/
    batch: batch/
    temp: temp/
    report: report/
    website: website/
